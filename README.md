You can create an APK as follows:

```
git clone https://github.com/ollm/admob-canvas-banner.git
cordova build --debug android
cp platforms/android/app/build/outputs/apk/debug/app-debug.apk app-debug.apk
```
